map = zoom in
map r recolor
map c toggle_page_mode
map d scroll half-down
map u scroll half-up

# c-n to toggle statusbar

set guioptions none

set recolor                     "true"
set recolor-keephue             "true"

include t-rosepinedawn
